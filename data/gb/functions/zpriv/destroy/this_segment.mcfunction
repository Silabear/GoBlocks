#
# Destroy segment
#
# [gb.block] [gb.block.repeat] [gb.block.if] [gb.segment] [gb.repeat_segment] [gb.if_segment]
# if repeat/if segment not needed anymore
# as/at segment marker
#

# remove segment
kill @s
fill ~ ~ ~ ~-1 ~ ~ air replace