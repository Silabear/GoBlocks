#
# Place repeat segment
#
# [gb.block] [gb.block.repeat] [gb.segment] [gb.repeat_segment]
# if new repeat segment is needed
# as spawn marker
# at blockcenter [aligned from spawn marker]
#

# construct segment
fill ~ ~ ~ ~-1 ~ ~ blue_glazed_terracotta