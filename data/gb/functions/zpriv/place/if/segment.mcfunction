#
# Place if segment
#
# [gb.block] [gb.block.if] [gb.segment] [gb.if_segment]
# if new if segment is needed
# as spawn marker
# at blockcenter [aligned from spawn marker]
#

# construct segment
fill ~ ~ ~ ~-1 ~ ~ minecraft:red_glazed_terracotta